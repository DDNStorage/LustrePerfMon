SUBDIRS = collectd dependency grafana xml_definition montools package .

EXTRA_DIST = version-gen.sh autogen.sh detect-distro.sh

maintainer-clean-local:
	-rm -f -r libltdl
	-rm -f INSTALL
	-rm -f aclocal.m4
